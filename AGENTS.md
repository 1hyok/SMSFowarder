# 도구와 검증

- 브라우저보다 가능한 CLI와 API를 먼저 사용한다.
- 변경 범위의 누락된 테스트를 확인하고, Gradle 빌드와 테스트는 JDK 21로 실행한다.
- Android 동작 확인에는 adb QA를 적재적소에 사용한다.
- SMS 실제 송수신 검증에는 통신 가능한 물리 기기와 회선이 필요하다. 에뮬레이터 결과만으로 실제 송수신을 검증했다고 판단하지 않는다.

# 비밀과 배포 경계

- `local.properties`, 서명 키, 토큰, 전화번호 같은 비밀·개인정보를 커밋하거나 로그에 노출하지 않는다.
- 서명, 스토어 등록, 외부 업로드와 배포는 사용자가 명시한 범위에서만 수행한다.
